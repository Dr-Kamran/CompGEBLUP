% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_env_kernels.R
\name{build_env_kernel_multi}
\alias{build_env_kernel_multi}
\title{Build Multi-Covariate Environmental Kernel}
\usage{
build_env_kernel_multi(covariate_matrix, bandwidth = NULL)
}
\arguments{
\item{covariate_matrix}{Matrix or data frame of covariates (rows = environments,
columns = covariates). Row names must be environment IDs.}

\item{bandwidth}{Optional bandwidth (scalar or per-covariate vector)}
}
\value{
Symmetric positive semi-definite matrix (e × e)
}
\description{
Constructs an e × e environmental similarity matrix from multiple
covariates simultaneously, using a multivariate Gaussian kernel.
}
\examples{
\dontrun{
# Combine multiple covariates into one kernel
cov_mat <- data.frame(
  temp = weather$fill_temp,
  vpd = weather$mean_vpd,
  row.names = weather$ENV
)
Omega_combined <- build_env_kernel_multi(cov_mat)
}
}
