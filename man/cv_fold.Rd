% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_cross_validation.R
\name{cv_fold}
\alias{cv_fold}
\title{Cross-Validation Fold}
\usage{
cv_fold(
  gdata,
  K_matrices,
  effects,
  fold,
  ridge,
  ridge_per_matrix = NULL,
  recompute_K = TRUE,
  verbose = FALSE,
  fixed = NULL,
  env_kernels = NULL
)
}
\arguments{
\item{gdata}{GBLUPData object}

\item{K_matrices}{List of relationship matrices (pre-computed, used when recompute_K = FALSE)}

\item{effects}{Character vector of effects}

\item{fold}{List with train and test indices}

\item{ridge}{Ridge parameter (scalar fallback for all matrices)}

\item{ridge_per_matrix}{Optional named numeric vector of per-matrix ridge values
(names: "A", "D", "AA", "AE", "DE", "AAE"). Overrides scalar \code{ridge}
for named components. Only used when \code{recompute_K = TRUE}.}

\item{recompute_K}{Logical, if TRUE recomputes K from training genotypes only}

\item{verbose}{Logical}

\item{fixed}{Optional fixed effects specification (formula or matrix)}

\item{env_kernels}{Optional named list of environmental similarity matrices (Omega)
for covariate-specific G×E effects (\code{AE_*}). Passed through to \code{fit_gblup()}.
These are environment-level kernels and are identical across folds, so they are used
as-is even when \code{recompute_K = TRUE} (only the genomic kernels are refit per fold).}
}
\value{
List with fold results
}
\description{
Fits model on training data and predicts test data for one fold.
}
\keyword{internal}
