% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_multi_trait.R
\name{estimate_multivariate_varcomp}
\alias{estimate_multivariate_varcomp}
\title{Estimate Multivariate Variance Components via EM-REML}
\usage{
estimate_multivariate_varcomp(
  Y,
  K,
  nIters = 500,
  tol = 1e-06,
  alpha = 0.7,
  verbose = FALSE
)
}
\arguments{
\item{Y}{Matrix of trait values (individuals x traits), should be centered/scaled}

\item{K}{Relationship matrix (n x n)}

\item{nIters}{Maximum EM iterations (default 500)}

\item{tol}{Convergence tolerance on relative log-likelihood change (default 1e-6)}

\item{alpha}{EM damping factor in (0, 1]. Lower = more damping = slower but stable.
Default 0.7 works well for most cases.}

\item{verbose}{Print progress every 10 iterations}
}
\value{
List with Vg, Ve, converged flag, iteration count, and REML log-likelihood
}
\description{
Uses EM-REML with spectral decomposition and step-size damping to estimate
genetic and residual covariance matrices for multi-trait models.
}
\details{
The algorithm works in the spectral space of the kinship matrix K = UDU'.
Transformed data Y* = U'Y yields independent observations with
Var(y*_i) = d_i * Vg + Ve.

The EM-REML updates follow Meyer (1997) and Lee & van der Werf (2006):
\deqn{Vg_{new} = (1/n) \sum_i [Vg - d_i Vg V_i^{-1} Vg + d_i Vg V_i^{-1} y_i y_i' V_i^{-1} Vg]}
\deqn{Ve_{new} = (1/n) \sum_i [Ve - Ve V_i^{-1} Ve + Ve V_i^{-1} y_i y_i' V_i^{-1} Ve]}

Step-size damping prevents oscillation:
\deqn{Vg = \alpha * Vg_{new} + (1-\alpha) * Vg_{old}}
}
\keyword{internal}
