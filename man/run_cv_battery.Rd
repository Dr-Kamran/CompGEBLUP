% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_workflows.R
\name{run_cv_battery}
\alias{run_cv_battery}
\title{Run Cross-Validation Battery}
\usage{
run_cv_battery(
  gdata,
  K_matrices,
  models = NULL,
  schemes = c("CV1", "CV2"),
  n_folds = 5,
  n_reps = 3,
  ridge = 0.001,
  ridge_per_matrix = NULL,
  verbose = TRUE
)
}
\arguments{
\item{gdata}{GBLUPData object (raw or 2-stage BLUEs).}

\item{K_matrices}{List of relationship matrices.}

\item{models}{Named list of model configurations. Each element is a list with
\code{effects} (character vector) and optionally \code{fixed}, \code{gdata}
(to override per-model), \code{ridge_per_matrix}.
Default: runs M1 through M5 with standard settings.}

\item{schemes}{Character vector of CV schemes: \code{"CV1"} (random 5-fold),
\code{"CV2"} (leave-one-env-out). Default: \code{c("CV1", "CV2")}.}

\item{n_folds}{Number of folds for CV1 (default: 5).}

\item{n_reps}{Number of repetitions (default: 3).}

\item{ridge}{Base ridge (default: 0.001).}

\item{ridge_per_matrix}{Named vector of per-matrix ridge values.}

\item{verbose}{Print progress (default TRUE).}
}
\value{
Data frame with columns: Model, Scheme, Rep, Fold, PA (predictive ability).
}
\description{
Runs a standardised set of cross-validation schemes across multiple model
configurations. Returns a structured data frame of results for direct
publication.
}
\examples{
\dontrun{
cv_results <- run_cv_battery(gdata, K,
                              models = list(M1 = list(effects = "A")))
}
}
